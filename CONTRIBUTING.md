# 🦉 راهنمای مشارکت در پروژه Minerva (مینروا)

سلام تیم! 👋 
این فایل توضیح می‌ده که چطور روی پروژه کار کنیم تا کارمون منظم بمونه و کدهای همدیگه رو خراب نکنیم. حتی اگه با Git خیلی راحت نیستید، این راهنما رو قدم‌به‌قدم بخونید. هدف اینه که همه‌ی ما یک زبان و روال مشترک داشته باشیم.

 ⚠️ **خلاصه‌ی طلایی:**  **هیچ‌وقت مستقیماً روی `main` یا `develop` پوش (Push) نکنید!**  همیشه یک شاخه‌ی جدید بسازید، کارتون رو انجام بدید، Pull Request بزنید، منتظر Review بمونید و بعد Merge کنید.

---

## ۱)  پروژه چیه؟

**مینروا (Minerva)** یک سامانه‌ی هوشمند تحت‌وب برای **مدیریت یکپارچه‌ی منابع انسانی و مالی (HRM & ERP)** شرکت‌هاست.

- 🐍 **Backend:** Django DRF (Python)
- ⚛️ **Frontend:** React 18
- 🐘 **Database:** PostgreSQL
- 🐳 **DevOps:** Docker

---

## ۲)  راه‌اندازی محیط (اولین باری که می‌خوای کار کنی)

### 🛠️ پیش‌نیازها
مطمئن بشید این موارد روی سیستمتون نصب باشن:
- Python (نسخه ۳.۱۱ یا بالاتر)
- Node.js (نسخه ۱۸ یا بالاتر)
- PostgreSQL (نسخه ۱۴ یا بالاتر)
- Git

###  گرفتن پروژه
```bash
git clone <repo-url>
cd minerva

```

###  راه‌اندازی Backend (Django)

```bash
cd backend
python -m venv venv         # ساخت محیط مجازی پایتون
source venv/bin/activate    # روی ویندوز: venv\Scripts\activate
pip install -r requirements.txt

```

یک فایل `.env` از روی نمونه بسازید:

```bash
cp .env.example .env

```

مقادیر دیتابیس رو داخلش پر کنید و بعد:

```bash
python manage.py migrate    # ساخت جدول‌های دیتابیس
python manage.py runserver  # اجرای سرور روی http://localhost:8000

```

### 🎨 راه‌اندازی Frontend (React)

```bash
cd frontend
npm install
npm run dev                 # اجرا روی http://localhost:5173 (یا پورت مشابه)

```

> 💡 **نکته:** اگه به مشکل خوردید، اول از همه چک کنید که PostgreSQL در حال اجراست و مقادیر `.env` درست وارد شده باشن.

---

## ۳)  مدل شاخه‌ها (Branching Model)

ما دو شاخه‌ی اصلی داریم که **مستقیماً روشون کار نمی‌کنیم**:

* 🟢 **`main`** ← نسخه‌ی پایدار و نهایی. همیشه باید سالم باشه.
* 🟡 **`develop`** ← نسخه‌ی در حال توسعه. کارهای تموم‌شده اینجا جمع می‌شن.

برای هر کاری (قابلیت جدید، رفع باگ و...) یک **شاخه‌ی جدید** از `develop` می‌سازیم.

```text
main
 └── develop
      ├── feature/employee-crud
      ├── feature/payroll-calc
      └── fix/login-error

```

---

## ۴)  نام‌گذاری شاخه‌ها

اسم شاخه باید بگه که دقیقاً دارید چی‌کار می‌کنید. فرمت:
**`<نوع>/<توضیح-کوتاه-با-خط-تیره>`**

| نوع | کاربرد | مثال |
| --- | --- | --- |
| ✨ `feature` | قابلیت جدید | `feature/leave-request` |
| 🐛 `fix` | رفع باگ | `fix/salary-rounding` |
| ♻️ `refactor` | بهبود کد بدون تغییر رفتار | `refactor/auth-service` |
| 📚 `docs` | مستندات | `docs/api-readme` |
| 🧪 `test` | اضافه‌کردن تست | `test/payroll-unit` |
| 🔧 `chore` | کارهای جانبی (وابستگی‌ها، تنظیمات) | `chore/update-deps` |

**قوانین:**

* فقط حروف کوچک انگلیسی و خط تیره (`-`) استفاده کنید.
* کوتاه و واضح بنویسید.
* **اصلاً** از فارسی یا فاصله (Space) توی اسم شاخه استفاده نکنید.

---

## ۵) 📝 قوانین Commit

ما از سبک **Conventional Commits** استفاده می‌کنیم. یعنی هر پیام کامیت با یک کلمه شروع می‌شه که نشون‌دهنده‌ی نوع تغییره.

فرمت: **`<نوع>: <توضیح کوتاه به زبان ساده>`**

**انواع مجاز:**

* `feat:` یک قابلیت جدید
* `fix:` رفع باگ
* `docs:` تغییر مستندات
* `style:` تغییر ظاهری کد (فاصله، فرمت) بدون تغییر منطق
* `refactor:` بازنویسی کد بدون تغییر در عملکرد
* `test:` اضافه یا اصلاح تست‌ها
* `chore:` کارهای جانبی نگهداری

✅ **مثال‌های خوب:**

```bash
git commit -m "feat: add employee creation form"
git commit -m "fix: correct overtime calculation for night shifts"
git commit -m "docs: update setup instructions in README"

```

❌ **مثال‌های بد (این‌کارو نکنید):**

```bash
git commit -m "تغییرات"
git commit -m "update"
git commit -m "final final v2 last"

```

> 💡 هر کامیت باید یک کار مشخص رو انجام بده. زیاد و کوچیک کامیت کنید! پیام رو با فعل و به انگلیسی ساده بنویسید.

---

## ۶)  روند کار روزانه (مهم‌ترین بخش)

هر بار که می‌خواید روی یک تسک جدید کار کنید، این مراحل رو دقیقاً دنبال کنید:

**۱. شاخه‌ی `develop` رو به‌روز کنید:**

```bash
git checkout develop
git pull origin develop

```

**۲. یک شاخه‌ی جدید بسازید:**

```bash
git checkout -b feature/leave-request

```

**۳. تغییرات رو اعمال و کامیت کنید:**

```bash
git add <فایل‌هایی که عوض کردید>
git commit -m "feat: add leave request model"

```

*(به‌جای `git add .` بهتره فایل‌ها رو با اسم اضافه کنید تا چیز اضافه‌ای commit نشه)*

**۴. شاخه‌تون رو پوش کنید:**

```bash
git push -u origin feature/leave-request

```

**۵. روی گیت‌هاب Pull Request (PR) بزنید** (حتماً به سمت `develop`).

**۶. منتظر بمانید تا یکی از اعضای تیم کد رو Review و Merge کنه.**

---

## ۷)  زدن Pull Request (PR)

وقتی PR می‌زنید:

* **عنوان** کوتاه و واضح باشه (مثلاً: `feat: leave request module`).
* **توضیحات PR** رو کامل بنویسید:
* چی کار کردید؟
* چطور تستش کردید؟
* اگه چیزی ناقصه بنویسید.


* PR رو **همیشه به سمت `develop**` بزنید.
* سعی کنید PRها کوچیک باشن تا Review کردنشون راحت باشه.

---

## ۸)  بازبینی کد (Code Review)

وقتی کد هم‌تیمی‌ها رو بررسی می‌کنید:

* مودب و سازنده باشید. پیشنهاد بدید، نه دستور.
* اگه چیزی واضح نیست، حتماً سؤال بپرسید.
* دنبال این‌ها بگردید: باگ، کد تکراری، اسم‌های گیج‌کننده، نبود تست.
* کمال‌گرایی بیش‌ازحد سرعت تیم رو می‌گیره؛ اگه مشکل جدی‌ای نبود تایید کنید.

> 🛑 **قانون تیم:** حداقل **۱ نفر** باید PR رو تأیید (Approve) کنه تا Merge بشه.

---

## ۹)  استایل و قوانین کدنویسی

###  Backend (Python / Django)

* از استاندارد **PEP 8** پیروی کنید.
* برای فرمت خودکار از **black** و برای بررسی از **flake8** استفاده می‌کنیم.
* برای مقادیر پولی **همیشه** از `Decimal` استفاده کنید، هیچ‌وقت `float`.

###  Frontend (React)

* از **ESLint** و **Prettier** برای تمیزی کد استفاده می‌کنیم.
* اسم کامپوننت‌ها باید PascalCase باشه (مثال: `EmployeeCard`).
* اسم فایل کامپوننت با اسم خودش یکی باشه.

###  قانون کلی

* کد رو قبل از PR یک‌بار خودتون بخونید.
* کامنت بذارید، ولی فقط جایی که منطق پیچیده‌ست.
* کدهای مرده و `console.log` یا `print`های اضافه رو پاک کنید.

---

## ۱۰)  چیزهایی که هرگز نباید Commit بشن!

فایل‌های زیر نباید وارد ریپازیتوری بشن (باید توی `.gitignore` باشن):

* فایل `.env` (و هر چیزی که توش رمز، توکن یا کلید هست)
* پوشه‌های `venv/` و `node_modules/`
* فایل‌های دیتابیس محلی (`db.sqlite3`)
* تنظیمات ادیتورها (`.vscode/`, `.idea/`)

> 🚨 اگه اشتباهی فایل حساسی (مثل رمز) commit شد، **سریعاً به تیم خبر بدید**.

---

## ۱۱)  حل تداخل (Merge Conflict)

اگه موقع merge با تداخل روبه‌رو شدید، نترسید. فقط معنیش اینه که دو نفر هم‌زمان یک بخش از کد رو تغییر دادن.

۱. آخرین وضعیت `develop` رو بگیرید و توی شاخه‌ی خودتون ادغام کنید:

```bash
git checkout feature/my-branch
git pull origin develop

```

۲. فایل‌های دارای تداخل رو باز کنید. بلوک‌هایی شبیه این می‌بینید:

```text
<<<<<<< HEAD
کد شما
=======
کد تغییر یافته در develop
>>>>>>> develop

```

۳. تصمیم بگیرید کدوم بخش بمونه (یا ترکیبشون کنید) و نشانگرها (`<<<`, `===`, `>>>`) رو پاک کنید.

۴. فایل‌ها رو اضافه و کامیت کنید:

```bash
git add <فایل_حل_شده>
git commit
git push

```

---

## ۱۲) ⚡ جمع‌بندی سریع (Cheat Sheet)

```bash
# ۱. آپدیت کردن develop
git checkout develop && git pull origin develop

# ۲. ساخت شاخه جدید برای کار
git checkout -b feature/something

# ۳. انجام کار و کامیت
git add <فایل‌های تغییرکرده> && git commit -m "feat: ..."

# ۴. ارسال به گیت‌هاب
git push -u origin feature/something

# ۵. ثبت PR روی گیت‌هاب به سمت develop

```
